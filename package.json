{
  "name": "lean-git",
  "version": "1.0.0",
  "description": "git init\r add new line (seconds commit)\r checkout new branch\r =============\r git reset --soft <commit id> =>reset to specific commit id and put commits from after it to staged\r git reset --mixed <commit id> =>reset to specific commit id and unstaged all commits after it\r git commit --hard <commit id> => reset to specific commit id and remove all commits after it",
  "main": "Cat.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.1"
  }
}
